<Window x:Class="DEM_1Project.DashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Панель управления" Height="450" Width="800"
        WindowStartupLocation="CenterScreen" Icon="/Assets/app.ico">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="LightGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0" 
                   Source="/Assets/logo.png" 
                   Width="150" Height="50" 
                   Margin="5"/>

            <TextBlock x:Name="lblUserInfo" 
                       Grid.Column="1" 
                       VerticalAlignment="Center" 
                       FontWeight="Bold" 
                       Margin="5"/>

            <Button x:Name="btnBackToMain" 
                    Grid.Column="2" 
                    Content="На главную" 
                    Click="btnBackToMain_Click" 
                    Padding="10,5" Margin="5"/>
        </Grid>

        <Grid Grid.Row="1" Margin="10">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Center" 
                        Margin="0,0,0,10">
                <Button x:Name="btnProducts" 
                        Content="Товары" 
                        Click="btnProducts_Click" 
                        Width="150" Height="40" 
                        Margin="5"/>
                <Button x:Name="btnOrders" 
                        Content="Заявки" 
                        Click="btnOrders_Click" 
                        Width="150" Height="40" 
                        Margin="5"/>
            </StackPanel>

            <Frame x:Name="MainFrame" 
                   Grid.Row="1" 
                   NavigationUIVisibility="Hidden"/>
        </Grid>
    </Grid>
</Window>